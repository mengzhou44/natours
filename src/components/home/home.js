import React from 'react';
import Header from '../_common/header';

export default (props) => {
    return (
        <div>
            <Header />
            <h1> Home Page </h1>
        </div>
    );
}